- [x] `add` cli tool.
    - [ ] ~~Make a cache system so the user wouldn't have to keep downloading the package from PyPI.~~
        - [ ] ~~Creating the cache~~
            - [ ] ~~Store the package in the cache.~~
                - [ ] ~~PATH: `~/.config/dev.pages.codedave.litv/cached_packages/flask/`~~
            - [ ] ~~Include the required deps, package_name, version, date, and content in a toml file.~~
                - [ ] ~~PATH: `~/.config/dev.pages.codedave.litv/cached_packages/flask/info.toml`~~
                - [ ] ~~PATH: `~/.config/dev.pages.codedave.litv/cached_packages/deps.toml`~~
        - [ ] ~~If possible, use symlink to reduce storage usage.~~
    - [ ] ~~Lock file.~~
    - [x] When a dependency is added, place it on `pyproject.toml` at `dependencies = []`.
- [x] `delete` cli tool.
    - [x] Removes a dependency in `pyproject.toml` and in the virtual environment too.
- [ ] `run` cli tool.
    - [ ] ~~Check if the user has the exact same dependencies. If they don't, sync them.~~
    - [x] If the user doen't have a virtual environment, automatically create one, and then run the app.
- [ ] `init` cli tool.
    - [x] Initialize the file structure:
        - [x] README.md
        - [x] .gitignore
        - [x] src/main.py
        - [x] README.md
        - [x] pyproject.toml
    - [ ] Initialize a git project/repo
- [x] global.
    - [x] Instead of activiating the virtual environment, just automatically run from `venv\\Scripts\\python.exe` from windows, and `venv/bin/python` in unix.
    - [ ] ~~Same thing as above, but for `pip`.~~ ? don't remember what this one was.